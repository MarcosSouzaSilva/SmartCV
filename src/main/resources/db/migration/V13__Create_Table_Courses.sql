CREATE TABLE courses (
                           id CHAR(36) PRIMARY KEY,
                           course VARCHAR(255),
                           institution VARCHAR(255),
                           moth VARCHAR(50),
                           graduation_year INT,
                           user_id CHAR(36)

);